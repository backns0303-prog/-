# 대시보드 앱 소개 발표 초안

## 1. 한 줄 소개

이 앱은 `엑셀 파일로 흩어진 수주/진행/재고 데이터를 업로드해서`, `Google Sheets 기준으로 정리하고`, `캘린더 중심 화면에서 일정과 품목을 한 번에 확인`할 수 있게 만든 운영형 대시보드입니다.

## 2. 발표 흐름

### 슬라이드 1. 제목

`품평/프로젝트 관리 대시보드`

- 엑셀 기반 운영 데이터를 한 화면으로 연결한 대시보드
- 업로드, 집계, 일정 확인, 상세 품목 추적까지 한 흐름으로 처리

발표 멘트:

오늘 소개드릴 앱은 현업에서 자주 쓰는 엑셀 데이터를 그대로 활용하면서도, 수주 진행 상황과 재고 상태를 더 빠르게 읽을 수 있도록 만든 대시보드입니다.

### 슬라이드 2. 왜 만들었는가

`문제`

- 데이터가 여러 엑셀 파일에 나뉘어 있음
- 수주 정보, 관리 정보, 생산 진행, 재고 현황을 따로 봐야 함
- 일정 확인과 품목 확인이 한 번에 이어지지 않음

`목표`

- 업로드만 하면 최신 데이터를 자동 반영
- 월별 일정과 프로젝트를 직관적으로 확인
- 필요한 수주건만 바로 drill-down

발표 멘트:

기존에는 필요한 정보를 찾기 위해 여러 파일과 시트를 옮겨 다녀야 했습니다. 그래서 이 앱은 데이터를 모으는 일보다 데이터를 읽고 판단하는 일에 집중하게 만드는 데 목적이 있습니다.

### 슬라이드 3. 핵심 사용자 흐름

`업무 흐름`

1. 엑셀 파일 업로드
2. Google Sheets에 최신 워크시트 생성
3. 최신 데이터 자동 선택 및 병합
4. 요약 카드와 캘린더에서 이상 징후 확인
5. 수주건 상세와 품목 목록으로 바로 drill-down
6. 필요하면 결과를 다시 엑셀로 다운로드

발표 멘트:

이 앱은 단순 조회 화면이 아니라, 업로드부터 확인, 상세 분석, 재다운로드까지 실제 업무 흐름을 끊기지 않게 연결한 것이 핵심입니다.

### 슬라이드 4. 메인 화면 1: 요약 카드

`한눈에 보는 핵심 지표`

- 주간/월간/임박 건수 같은 요약 카드 제공
- 카드 클릭 시 관련 수주건만 바로 상세 조회
- 단순 숫자 표시가 아니라 탐색 시작점 역할

발표 멘트:

상단 요약 카드는 숫자를 보여주는 데서 끝나지 않습니다. 사용자가 지금 어디를 먼저 봐야 하는지 알려주고, 클릭하면 그 기준으로 바로 상세 화면이 열리도록 만들었습니다.

### 슬라이드 5. 메인 화면 2: 오버레이 캘린더

`일정 중심 탐색`

- 월 단위로 프로젝트 일정을 겹쳐서 표시
- 사업 구분, 월, 제품군 조건에 따라 즉시 필터링
- 여러 수주건을 함께 선택해서 비교 가능

발표 멘트:

캘린더는 단순 날짜 표시가 아니라, 실제로 어떤 프로젝트가 어느 시점에 몰려 있는지 보는 운영 화면입니다. 특히 여러 건이 겹칠 때 병목이나 우선순위를 빠르게 파악할 수 있습니다.

### 슬라이드 6. 메인 화면 3: 상세 패널과 품목 표

`수주건 단위 drill-down`

- 선택한 수주건의 시작/종료일, 수량, 재고금액 확인
- 관련 세부 수주건을 체크박스로 세밀하게 필터링
- 품목 표를 필터링한 뒤 `.xlsx`로 다시 다운로드 가능

발표 멘트:

상세 화면에서는 화면 하나로 일정, 수량, 재고, 품목까지 이어서 볼 수 있습니다. 보기에서 끝나지 않고 다시 엑셀로 내보낼 수 있어서 현업 전달도 쉽습니다.

### 슬라이드 7. 데이터 처리 방식

`보이지 않는 핵심`

- 업로드한 `.xls`, `.xlsx` 파일을 유형별로 분류
- Google Sheets에 시점별 워크시트 생성
- 최신 워크시트를 기준으로 자동 병합
- 수주, 관리, 진행, 재고 데이터를 공통 키로 연결

발표 멘트:

사용자는 화면만 보지만, 뒤에서는 데이터 정리 로직이 꽤 중요합니다. 어떤 파일이 어떤 데이터인지 분류하고, 최신 시트를 자동 선택해 한 번에 결합하는 구조가 앱의 신뢰도를 좌우합니다.

### 슬라이드 8. 구현 포인트

`기술적으로 신경 쓴 부분`

- `Streamlit` 기반으로 빠르게 화면 구현
- 커스텀 컴포넌트로 캘린더/요약카드 인터랙션 강화
- `st.cache_resource`, `st.cache_data`로 로딩 비용 절감
- 병렬 로딩으로 스프레드시트 읽기 성능 개선
- 운영 중 기준값과 키워드를 화면에서 수정 가능

발표 멘트:

단순히 화면을 만든 것이 아니라, 반복 조회가 빠르게 느껴지도록 캐시와 병렬 처리도 넣었습니다. 또 북미 키워드나 필터 기준처럼 운영자가 자주 바꾸는 설정은 코드 수정 없이 화면에서 관리할 수 있게 했습니다.

### 슬라이드 9. 기대 효과

`효과`

- 엑셀 파일을 일일이 열어보는 시간 감소
- 일정과 품목 이슈를 더 빨리 탐지
- 회의나 공유 시 같은 기준 화면으로 소통 가능
- 업로드와 조회가 한 도구 안에서 이어져 업무 흐름 단순화

발표 멘트:

이 앱의 가치는 데이터를 더 많이 보여주는 데 있지 않습니다. 같은 데이터를 더 빨리 읽고, 더 빨리 공유하고, 더 빨리 다음 행동으로 이어지게 만드는 데 있습니다.

### 슬라이드 10. 마무리

`정리`

- 엑셀 친화적인 운영 데이터 환경을 유지
- Google Sheets를 중간 허브로 활용
- 일정, 수주, 재고, 품목을 하나의 화면으로 연결

마무리 멘트:

정리하면 이 대시보드는 기존 엑셀 중심 업무를 완전히 버리지 않으면서도, 실제 의사결정과 운영 확인이 더 쉬워지도록 연결해 주는 도구라고 소개할 수 있습니다.

## 3. 3분 발표용 초압축 버전

안녕하세요. 오늘 소개드릴 앱은 엑셀로 관리되던 수주, 진행, 재고 데이터를 하나의 흐름으로 연결한 품평/프로젝트 관리 대시보드입니다.

기존에는 여러 파일과 시트를 따로 열어야 해서 일정 확인, 품목 확인, 재고 확인이 끊겨 있었습니다. 이 앱은 엑셀 파일을 업로드하면 Google Sheets에 최신 데이터가 반영되고, 그 데이터를 기반으로 요약 카드, 월간 캘린더, 상세 품목 표까지 바로 이어서 볼 수 있게 만들었습니다.

특히 캘린더에서 프로젝트 일정을 겹쳐 보면서 필요한 수주건만 선택할 수 있고, 상세 패널에서는 수량과 재고금액, 관련 품목을 다시 확인한 뒤 엑셀로 재다운로드할 수 있습니다.

기술적으로는 Streamlit 기반에 커스텀 컴포넌트, 캐시, 병렬 데이터 로딩을 넣어서 운영 도구로 쓰기 좋게 구성했습니다.

한마디로 정리하면, 이 앱은 엑셀 기반 업무를 유지하면서도 데이터를 훨씬 빠르게 읽고 판단하게 해주는 실무형 대시보드입니다.

## 4. 발표할 때 강조하면 좋은 키워드

- 실무형
- 업로드부터 조회까지 한 흐름
- 일정 중심 탐색
- 클릭 기반 drill-down
- 엑셀 친화적
- 운영 속도 개선
